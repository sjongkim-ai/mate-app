# Hello World Node.js Project

This is a simple Node.js project that logs "Hello, World!" to the console.

## Running the Project

To run the project, use the following command:

```
npm start
```

This will execute `node index.js` and print "Hello, World!" to the console.